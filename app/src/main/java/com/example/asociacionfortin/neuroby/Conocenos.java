package com.example.asociacionfortin.neuroby;

import android.content.res.Resources;
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.text.Html;
import android.widget.TextView;

public class Conocenos extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_conocenos);


        /*

        Resources res = getResources();

        String texto = res.getString(R.string.text_integrantes_conocenos);

        CharSequence textoInterpretado = Html.fromHtml(texto);

        TextView textView = (TextView)findViewById(R.id.textView);
        textView.setText(textoInterpretado);


*/


    }
}
