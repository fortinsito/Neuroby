<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asociacionfortin.neuroby">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ActivityPrincipal"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Asignaturas"
            android:label="Asignaturas"
            android:parentActivityName=".ActivityPrincipal" />
        <activity
            android:name=".RelajaLaNeurona"
            android:label="Relaja la neurona"
            android:parentActivityName=".ActivityPrincipal"/>
        <activity
            android:name=".Ayuda"
            android:label="Ayuda"
            android:parentActivityName=".ActivityPrincipal" />
        <activity
            android:name=".Calculo"
            android:label="@string/title_activity_calculo"
            android:parentActivityName=".Asignaturas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Conocenos"
            android:label="Conócenos"
            android:parentActivityName=".Ayuda" />
    </application>

</manifest>